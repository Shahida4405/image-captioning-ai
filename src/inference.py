import numpy as np

def generate_caption(model, tokenizer, img):
    """Fake quick captioning — no InceptionV3 download."""
    # Simulate extracted features (dummy data)
    fake_features = np.random.rand(1, 2048)

    # Placeholder caption (you can customize later)
    caption = "a lovely photo captured beautifully"
    return caption
